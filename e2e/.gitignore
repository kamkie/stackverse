# per-role login state captured by tests/auth.setup.ts
.auth/
test-results/
playwright-report/
*.log

# Yarn Berry PnP: lockfile and .yarnrc.yml are committed; the rest is
# generated by `yarn install`.
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
