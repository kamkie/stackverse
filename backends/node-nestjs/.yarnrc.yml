# Yarn Berry with Plug'n'Play, matching frontends/react, conformance/ and e2e/
# (no node_modules). The cache lives in-project so the Docker build can copy the
# resolved archives into the runtime image without a network fetch.
nodeLinker: pnp
enableGlobalCache: false
