# Build and unit tests for one implementation. Each implementation has its own
# build workflow file so parallel variant PRs never edit a shared workflow —
# adding a variant means adding a sibling file, nothing else. The contract
# suites (conformance/e2e) and the ci-ok merge gate live in ci.yml.
name: build-backend-play-scala

on:
  push:
    branches: [main]
  pull_request:
    paths:
      - '.github/workflows/build-backend-play-scala.yml'
      - 'backends/play-scala/**'
      - 'spec/openapi.yaml'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  backend-play-scala:
    name: Backend · Play Framework (Scala)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: backends/play-scala
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21
      - uses: sbt/setup-sbt@v1
      - uses: actions/cache@v6
        with:
          path: |
            ~/.ivy2/cache
            ~/.sbt
            ~/.cache/coursier
          key: sbt-${{ runner.os }}-backend-play-scala-${{ hashFiles('backends/play-scala/build.sbt', 'backends/play-scala/project/*.sbt', 'backends/play-scala/project/build.properties') }}
          restore-keys: sbt-${{ runner.os }}-backend-play-scala-
      - name: Test with coverage
        run: sbt clean coverage test coverageReport
      - name: Normalize coverage report path
        run: |
          report="$(find target -path '*/scoverage-report/scoverage.xml' -print -quit)"
          test -n "$report"
          mkdir -p target/scoverage-report
          cp "$report" target/scoverage-report/scoverage.xml
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v7
        continue-on-error: true
        with:
          files: backends/play-scala/target/scoverage-report/scoverage.xml
          flags: backend-play-scala
          token: ${{ secrets.CODECOV_TOKEN }}
      - name: Upload test results to Codecov
        if: ${{ !cancelled() }}
        uses: codecov/codecov-action@v7
        continue-on-error: true
        with:
          report_type: test_results
          directory: backends/play-scala/target/test-reports
          flags: backend-play-scala
          token: ${{ secrets.CODECOV_TOKEN }}

  # The dependency submission API requires contents: write. Keep PR builds
  # read-only and submit resolved sbt snapshots only from trusted main pushes.
  backend-play-scala-dependency-submission:
    name: Backend · Play Framework (Scala) dependency submission
    if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21
      - uses: sbt/setup-sbt@v1
      - name: Submit sbt dependency graph
        uses: scalacenter/sbt-dependency-submission@v3
        with:
          working-directory: backends/play-scala
          correlator: backend-play-scala
