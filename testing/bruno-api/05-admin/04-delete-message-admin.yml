info:
  name: Delete runtime message
  type: http
  seq: 4
  tags:
    - admin
    - messages

http:
  method: DELETE
  url: "{{BACKEND_URL}}/api/v1/messages/{{messageId}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("admin can delete the created message", function() {
          expect(res.status).to.equal(204);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
